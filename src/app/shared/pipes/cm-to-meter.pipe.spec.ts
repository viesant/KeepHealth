import { CmToMeterPipe } from './cm-to-meter.pipe';

describe('CmToMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new CmToMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
